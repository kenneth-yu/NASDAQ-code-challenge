const initialState = {
  currentUSD: 1000,
  currentBTC: 10,
  BTCtoUSD: 7887.99 //Conversion Rate from 1 BTC to USD
}

export default function reducer(state = initialState, action) {
  switch (action.type){
    default:
      return state
  }

}
